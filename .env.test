# Test-only placeholders. No real credentials.
# src/core/config/env.ts validates these at import time, so every value below
# must be present or importing any module that touches config throws.
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=test-anon-key-not-a-real-credential
DATABASE_URL=postgresql://postgres:postgres@localhost:54322/postgres
ANTHROPIC_API_KEY=test-key-not-a-real-credential
LOG_LEVEL=silent
